import * as Elements from "./scripts/ElementCreator";

export default function createMainPage() {
  const elementTree = [];
  elementTree.push(createElement("header", "main-header"));
  elementTree.push(createElement("div"));
}
